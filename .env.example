# Copy to `.env` and fill in.
# Defaults are picked up by playwright.config.ts.

BASE_URL=https://runwayml.com
API_BASE_URL=https://jsonplaceholder.typicode.com

# Optional: Enable verbose logging
DEBUG=0

# Optional: AI generation API keys (NEVER commit real keys)
# RUNWAY_API_KEY=
# PIKA_API_KEY=
# OPENAI_API_KEY=
